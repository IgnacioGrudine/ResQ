# Copy this file to .env and adjust values as needed.
# .env is gitignored — never commit real credentials.

# PostgreSQL credentials (used by docker-compose services)
POSTGRES_USER=resq
POSTGRES_PASSWORD=resq_dev
POSTGRES_DB=resq_db
