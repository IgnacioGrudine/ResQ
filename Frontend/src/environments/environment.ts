export const environment = {
  production: true,
  // El placeholder se reemplaza por la key real durante el build de Docker (ver Frontend/Dockerfile)
  googleMapsApiKey: '__GOOGLE_MAPS_API_KEY__'
};
